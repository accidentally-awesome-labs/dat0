[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
image = { version = "0.25", default-features = false, features = ["png"] }
# MX2: the perf gate reads `docs/internal/perf-baselines.json` and parses the
# one JSON line each scenario prints.
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

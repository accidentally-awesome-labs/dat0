//! SQL autocomplete `CompletionProvider` (P5b).
//!
//! Placeholder after the T0 spike: the spike proved the `lsp-types`/`ropey`
//! dep pins resolve and gpui-component's `CompletionProvider` trait signature
//! matches (the menu-render/insert visual exit-criterion is owed to human UAT,
//! deferred to P5c per the gate decision). The real provider lands in T1/T2.
